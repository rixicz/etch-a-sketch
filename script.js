const userInput = parseInt(prompt("Type in the size of the one side of the grid (e.g. input: 64, the grid is gonna be 64x64"))
if (userInput > 100) {
    alert("The size of the one side has to be less than 100.")
}